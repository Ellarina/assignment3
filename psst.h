#ifndef PSST_H
#define PSST_H
#include <string>

using namespace std;

class psst
{
    public:
        psst();
        virtual ~psst();
            void sort(string*, int);
    protected:
    private:
};

#endif // PSST_H
